// import axiosInstance from "../utils/axios";
// import { CONFIG } from "@/config/config";

// export const getAllConfigs = async (): Promise<any[]> => {
//   try {
//     const response = await axiosInstance.post(CONFIG.SCORE_API_ENDPOINTS.GET_ALL_CONFIGS, {
//       isActive: true,
//     });
//     return response.data || [];
//   } catch (error: any) {
//     console.error("Error in getAllConfigs:", error.message || error);
//     return [];
//   }
// };
